#include<stdio.h>
#include<conio.h>
int main()
{
int i,n[5],k,sum=0;
clrscr();
printf("enter the array elements are:");
for(i=0;i<n[i];i++)
scanf("%d",&i);
printf("enter the k value:");
scanf("%d",&k);
if(k<0)
{
sum=sum+n[i];
i++;
}
printf("the output is:",&sum);
getch();
return 0;
}
